# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLO11-Super-BiFPN-Att: Slim Params (~2.9M) + Triplet + CoordAtt

# Parameters
nc: 80
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# Backbone (Added TripletAttention)
backbone:
  - [-1, 1, Conv, [64, 3, 2]]             # 0
  - [-1, 1, Conv, [128, 3, 2]]            # 1
  - [-1, 2, C3k2, [256, False, 0.25]]     # 2
  - [-1, 1, Conv, [256, 3, 2]]            # 3
  - [-1, 2, C3k2, [512, False, 0.25]]     # 4 (Backbone P3)
  - [-1, 1, Conv, [512, 3, 2]]            # 5
  - [-1, 2, C3k2, [512, True]]            # 6 (Backbone P4)
  - [-1, 1, Conv, [1024, 3, 2]]           # 7
  - [-1, 2, C3k2, [1024, True]]           # 8 (Backbone P5)
  - [-1, 1, SPPF, [1024, 5]]              # 9
  - [-1, 2, C2PSA, [1024]]                # 10

  # 🔥 TripletAttention: Chốt chặn cuối cùng Backbone
  - [-1, 1, TripletAttention, []]         # 11 (Output P5 Backbone)

# Head (Slim BiFPN + CoordAtt)
head:
  # ================= Top-down =================
  # P5 -> P4
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 12
  - [[-1, 6], 1, BiFPN, [512]]                   # 13 (Hợp nhất với Backbone P4 layer 6)
  - [-1, 2, C3k2, [512, False]]                  # 14 (Feature P4 Top-down)

  # P4 -> P3
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 15
  - [[-1, 4], 1, BiFPN, [256]]                   # 16 (Hợp nhất với Backbone P3 layer 4)
  - [-1, 2, C3k2, [256, False]]                  # 17

  # 🔥 CoordAtt P3: Tinh chỉnh không gian cho vật nhỏ
  - [-1, 1, CoordAtt, []]                        # 18 -> Detect P3

  # ================= Bottom-up =================
  # P3 -> P4 (Super Node)
  - [-1, 1, Conv, [256, 3, 2]]                   # 19
  # Input updated:
  # 1. [-1]: P3 đi lên (Layer 19)
  # 2. [6]: Backbone P4 gốc
  # 3. [14]: P4 Top-down (Layer 14)
  - [[-1, 6, 14], 1, BiFPN, [512]]               # 20
  - [-1, 2, C3k2, [512, False]]                  # 21

  # 🔥 CoordAtt P4: Tinh chỉnh cho vật trung bình
  - [-1, 1, CoordAtt, []]                        # 22 -> Detect P4

  # P4 -> P5
  - [-1, 1, Conv, [512, 3, 2]]                   # 23
  # Input updated: [-1, 11] (Lấy từ TripletAttention layer 11)
  # Channel giữ Slim 512
  - [[-1, 11], 1, BiFPN, [512]]                  # 24
  - [-1, 2, C3k2, [512, True]]                   # 25

  # 🔥 CoordAtt P5: Tinh chỉnh cho vật lớn (Optional nhưng nên có cho đồng bộ)
  - [-1, 1, CoordAtt, []]                        # 26 -> Detect P5

  # Detect
  - [[18, 22, 26], 1, Detect, [nc]]              # Detect từ các lớp CoordAtt