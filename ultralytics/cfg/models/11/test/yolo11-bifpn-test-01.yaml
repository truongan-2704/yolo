# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLO11 OPTIMIZED: BiFPN P3-P5 (No P2 Noise)

nc: 80
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]             # 0 P1/2
  - [-1, 1, Conv, [128, 3, 2]]            # 1 P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]     # 2 P2/4
  - [-1, 1, Conv, [256, 3, 2]]            # 3 P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]     # 4 P3/8
  - [-1, 1, Conv, [512, 3, 2]]            # 5 P4/16
  - [-1, 2, C3k2, [512, True]]            # 6 P4/16
  - [-1, 1, Conv, [1024, 3, 2]]           # 7 P5/32
  - [-1, 2, C3k2, [1024, True]]           # 8 P5/32
  - [-1, 1, SPPF, [1024, 5]]              # 9 P5/32
  - [-1, 2, C2PSA, [1024]]                # 10 P5/32

head:
  # --- BiFPN Top-Down ---
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 11
  - [[-1, 6], 1, BiFPN, [512]]                 # 12 Fuse P5->P4 (Lưu ý: Để channel tự động hoặc 512 cho khớp)
  - [-1, 2, C3k2, [512, False]]                # 13

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 14
  - [[-1, 4], 1, BiFPN, [256]]                 # 15 Fuse P4->P3
  - [-1, 2, C3k2, [256, False]]                # 16

  # --- BiFPN Bottom-Up (Sửa đoạn này để bỏ Layer 17 cũ) ---
  - [-1, 1, Conv, [256, 3, 2]]                 # 17 Downsample P3
  - [[-1, 13], 1, BiFPN, [512]]                # 18 Fuse P3->P4 (Bỏ input từ layer 1 cũ)
  - [-1, 2, C3k2, [512, False]]                # 19

  - [-1, 1, Conv, [512, 3, 2]]                 # 20 Downsample P4
  - [[-1, 10], 1, BiFPN, [512]]                # 21 Fuse P4->P5 (Lấy input từ 10 - Backbone P5)
  - [-1, 2, C3k2, [512, True]]                 # 22

  # --- Detect ---
  - [[16, 19, 22], 1, Detect, [nc]]            # 23 Detect(P3, P4, P5)