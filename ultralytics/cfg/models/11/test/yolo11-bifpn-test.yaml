# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLO11 BALANCED: BiFPN + P2 Injection (Clean)

nc: 80
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

backbone:
  - [-1, 1, Conv, [64, 3, 2]]             # 0 P1/2
  - [-1, 1, Conv, [128, 3, 2]]            # 1 P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]     # 2 P2/4
  - [-1, 1, Conv, [256, 3, 2]]            # 3 P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]     # 4 P3/8
  - [-1, 1, Conv, [512, 3, 2]]            # 5 P4/16
  - [-1, 2, C3k2, [512, True]]            # 6 P4/16
  - [-1, 1, Conv, [1024, 3, 2]]           # 7 P5/32
  - [-1, 2, C3k2, [1024, True]]           # 8 P5/32
  - [-1, 1, SPPF, [1024, 5]]              # 9 P5/32
  - [-1, 2, C2PSA, [1024]]                # 10 P5/32

head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 11
  - [[-1, 6], 1, BiFPN, [256]]                   # 12 P4/16
  - [-1, 2, C3k2, [256, False]]                  # 13

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 14
  - [[-1, 4], 1, BiFPN, [256]]                   # 15 P3/8
  - [-1, 2, C3k2, [256, False]]                  # 16

  - [1, 1, Conv, [256, 3, 2]]                    # 17 P3/8 (from P2)
  - [[-1, 4, 16], 1, BiFPN, [256]]               # 18 P3/8
  - [-1, 2, C3k2, [256, False]]                  # 19

  - [-1, 1, Conv, [256, 3, 2]]                   # 20
  - [[-1, 6, 13], 1, BiFPN, [256]]               # 21 P4/16
  - [-1, 2, C3k2, [256, False]]                  # 22

  - [-1, 1, Conv, [512, 3, 2]]                   # 23
  - [[-1, 10], 1, BiFPN, [512]]                  # 24 P5/32
  - [-1, 2, C3k2, [512, True]]                   # 25

  - [[19, 22, 25], 1, Detect, [nc]]              # 26 Detect(P3, P4, P5)