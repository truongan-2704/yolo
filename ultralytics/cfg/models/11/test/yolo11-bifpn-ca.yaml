# Ultralytics YOLO 🚀, AGPL-3.0
# YOLO12 HYBRID: 1 CA Backbone (End) + Full CA Head + BiFPN + P2 Loop

nc: 80
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# =========================
# Backbone (1 CA ADDED AT END)
# =========================
backbone:
  - [-1, 1, Conv, [64, 3, 2]]             # 0
  - [-1, 1, Conv, [128, 3, 2]]            # 1
  - [-1, 2, C3k2, [256, False, 0.25]]     # 2 - P2/4
  - [-1, 1, Conv, [256, 3, 2]]            # 3
  - [-1, 2, C3k2, [512, False, 0.25]]     # 4 - P3/8
  - [-1, 1, Conv, [512, 3, 2]]            # 5
  - [-1, 2, C3k2, [512, True]]            # 6 - P4/16
  - [-1, 1, Conv, [1024, 3, 2]]           # 7
  - [-1, 2, C3k2, [1024, True]]           # 8
  - [-1, 1, SPPF, [1024, 5]]              # 9

  # 🔥 THÊM 1 CA VÀO CUỐI BACKBONE
  - [-1, 1, CoordAtt, []]                 # 10 (P5 Output Clean)

# =========================
# Head (FULL CA + BiFPN + P2 Loop)
# =========================
head:
  # === Top-down Path ===
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 11
  - [[-1, 6], 1, BiFPN, [256]]                   # 12 (Nối P4 Backbone)
  - [-1, 2, C3k2, [256, False]]                  # 13

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 14
  - [[-1, 4], 1, BiFPN, [256]]                   # 15 (Nối P3 Backbone)
  - [-1, 2, C3k2, [256, False]]                  # 16
  - [-1, 1, CoordAtt, []]                        # 17 (CA cho nhánh Top-down P3)

  # === P2 Loop (High Res) ===
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 18
  - [[-1, 2], 1, BiFPN, [256]]                   # 19 (Nối P2 Backbone)
  - [-1, 3, C3k2, [128, False]]                  # 20
  - [-1, 1, CoordAtt, []]                        # 21 (CA cho nhánh P2)

  # === Bottom-up Path ===

  # --- P3 Output ---
  - [-1, 1, Conv, [256, 3, 2]]                   # 22
  # Input BiFPN: [-1, 4, 17] (17 là output CA nhánh trên)
  - [[-1, 4, 17], 1, BiFPN, [256]]               # 23
  - [-1, 4, C3k2, [256, False]]                  # 24
  - [-1, 1, CoordAtt, []]                        # 25 -> Detect P3 (CA Final)

  # --- P4 Output ---
  - [-1, 1, Conv, [256, 3, 2]]                   # 26
  # Input BiFPN: [-1, 6, 13]
  - [[-1, 6, 13], 1, BiFPN, [256]]               # 27
  - [-1, 4, C3k2, [512, False]]                  # 28
  - [-1, 1, CoordAtt, []]                        # 29 -> Detect P4 (CA Final)

  # --- P5 Output ---
  - [-1, 1, Conv, [256, 3, 2]]                   # 30
  # Input BiFPN: [-1, 10] (Nối vào Layer 10 - CA Backbone mới thêm)
  - [[-1, 10], 1, BiFPN, [512]]                  # 31
  - [-1, 2, C3k2, [1024, True]]                  # 32
  - [-1, 1, CoordAtt, []]                        # 33 -> Detect P5 (CA Final)

  # Detect
  - [[25, 29, 33], 1, Detect, [nc]]