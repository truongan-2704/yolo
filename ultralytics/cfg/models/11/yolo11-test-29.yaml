# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLO11-Super-BiFPN-Slim: Optimized for Parameters (~2.8M)

# Parameters
nc: 80
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# Backbone (Giữ nguyên)
backbone:
  - [-1, 1, Conv, [64, 3, 2]]             # 0
  - [-1, 1, Conv, [128, 3, 2]]            # 1
  - [-1, 2, C3k2, [256, False, 0.25]]     # 2
  - [-1, 1, Conv, [256, 3, 2]]            # 3
  - [-1, 2, C3k2, [512, False, 0.25]]     # 4
  - [-1, 1, Conv, [512, 3, 2]]            # 5
  - [-1, 2, C3k2, [512, True]]            # 6
  - [-1, 1, Conv, [1024, 3, 2]]           # 7
  - [-1, 2, C3k2, [1024, True]]           # 8
  - [-1, 1, SPPF, [1024, 5]]              # 9
  - [-1, 2, C2PSA, [1024]]                # 10

# Head (Slimmed Down P5)
head:
  # === Top-down ===
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 11
  - [[-1, 6], 1, BiFPN, [512]]                   # 12
  - [-1, 2, C3k2, [512, False]]                  # 13

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 14
  - [[-1, 4], 1, BiFPN, [256]]                   # 15
  - [-1, 2, C3k2, [256, False]]                  # 16 -> Detect P3

  # === Bottom-up ===
  - [-1, 1, Conv, [256, 3, 2]]                   # 17
  # Vẫn giữ nguyên nút giao 3 nhánh (Super Node)
  - [[-1, 6, 13], 1, BiFPN, [512]]               # 18
  - [-1, 2, C3k2, [512, False]]                  # 19 -> Detect P4

  - [-1, 1, Conv, [512, 3, 2]]                   # 20
  # 👇 GIẢM Ở ĐÂY: 1024 -> 512
  # Input từ backbone 1024 sẽ được BiFPN nén xuống 512
  - [[-1, 10], 1, BiFPN, [512]]                  # 21
  - [-1, 2, C3k2, [512, True]]                   # 22 -> Detect P5

  # Detect
  - [[16, 19, 22], 1, Detect, [nc]]