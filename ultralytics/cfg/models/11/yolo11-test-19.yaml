# Ultralytics YOLO 🚀, AGPL-3.0
# YOLO12 ULTIMATE: BiFPN Active + P2 Loop + EMA Attention
# Designed for Maximum mAP on Small Objects

# Parameters
nc: 80
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# Backbone
backbone:
  - [-1, 1, Conv, [64, 3, 2]]             # 0
  - [-1, 1, Conv, [128, 3, 2]]            # 1
  - [-1, 2, C3k2, [256, False, 0.25]]     # 2
  - [-1, 1, Conv, [256, 3, 2]]            # 3
  - [-1, 2, C3k2, [512, False, 0.25]]     # 4
  - [-1, 1, Conv, [512, 3, 2]]            # 5
  - [-1, 2, C3k2, [512, True]]            # 6
  - [-1, 1, Conv, [1024, 3, 2]]           # 7
  - [-1, 2, C3k2, [1024, True]]           # 8
  - [-1, 1, SPPF, [1024, 5]]              # 9
  - [-1, 1, C2PSA, [1024]]                # 10
  # Thay MHSA bằng EMA để đồng bộ sức mạnh Attention
  - [-1, 1, EMA, [1024, 8]]               # 11 <--- Backbone EMA

# Head (BiFPN + EMA)
head:
  # === Top-down ===
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 12
  - [[-1, 6], 1, BiFPN, [256]]                   # 13
  - [-1, 2, C3k2, [256, False]]                  # 14

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 15
  - [[-1, 4], 1, BiFPN, [256]]                   # 16
  - [-1, 2, C3k2, [256, False]]                  # 17
  # Thêm EMA cho P3 (Quan trọng cho vật thể nhỏ)
  - [-1, 1, EMA, [256, 8]]                       # 18 (P3 EMA)

  # P2 Loop
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 19
  - [[-1, 2], 1, BiFPN, [256]]                   # 20
  - [-1, 3, C3k2, [128, False]]                  # 21
  # Thêm EMA cho P2 (Siêu quan trọng để lọc nhiễu chi tiết)
  - [-1, 1, EMA, [128, 8]]                       # 22 (P2 EMA)

  # === Bottom-up ===

  # P3 Output
  - [-1, 1, Conv, [256, 3, 2]]                   # 23
  - [[-1, 4, 18], 1, BiFPN, [256]]               # 24
  - [-1, 4, C3k2, [256, False]]                  # 25
  - [-1, 1, EMA, [256, 8]]                       # 26 (Output P3 + EMA)

  # P4 Output
  - [-1, 1, Conv, [256, 3, 2]]                   # 27
  - [[-1, 6, 14], 1, BiFPN, [256]]               # 28
  - [-1, 4, C3k2, [512, False]]                  # 29
  - [-1, 1, EMA, [512, 8]]                       # 30 (Output P4 + EMA)

  # P5 Output
  - [-1, 1, Conv, [256, 3, 2]]                   # 31
  - [[-1, 11], 1, BiFPN, [256]]                  # 32
  - [-1, 2, C3k2, [1024, True]]                  # 33

  # Detect (P3, P4, P5)
  - [[26, 30, 33], 1, Detect, [nc]]